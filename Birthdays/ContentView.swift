import SwiftUI

struct ContentView: View {
//    @State private var friends: [Friend] = [
//        Friend(name: "Julia", birthday: .now) ,
//        Friend(name: "Sammi", birthday: Date(timeIntervalSince1970: <#T##TimeInterval#>))
//    ]
    var body: some View {
        VStack {
            
            
            
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
